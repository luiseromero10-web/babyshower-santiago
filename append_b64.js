const fs = require('fs'); const path = require('path'); const target = path.resolve(process.argv[2]); const b64 = process.argv[3]; fs.mkdirSync(path.dirname(target), { recursive: true }); if (process.argv[4] === 'new') { fs.writeFileSync(target, Buffer.from(b64, 'base64')); } else { fs.appendFileSync(target, Buffer.from(b64, 'base64')); } console.log('Wrote/Appended:', process.argv[2]);